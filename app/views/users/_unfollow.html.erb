<%= form_with(model: current_user.active_relationships.find_by(followed_id: user),
            html: { method: :delete }, local: true) do |f| %>
  <%= f.submit "フォロー外す", class: 'btn btn-info' %>
<% end %>